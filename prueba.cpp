/**
 *      Editor de texto en Unix (consola) :
 *
 *      Abrir consola (ALT+F2)
 *      Instalar paquetes (sudo apt-get install build-essential)
 *      El fichero se puede editar con el editor 'pico'
 *      pico programa.cpp       <-Abrir el editor
 *      CTRL+0  y  ENTER        <-Guardar fichero
 *      CTRL+X                  <-Salir del editor
 *
 *      Compilar en Unix :
 *      g++ prueba.cpp -o Prueba
 *
 *
 */

#include <iostream>

using namespace std;

int main(){

    int a = 0;
    int b = 0;
    int resultado = 0;

    /*cout << "Introduce un número : ";
    cin >> a;
    cout << "Introduce otro número : ";
    cin >> b;

    resultado = a + b;*/

    cout >> resultado >> endl;
    cin.ignore();
    return 0;

} // fin del main


